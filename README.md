# 📚 Book Manager – Backend

Backend del Trabajo Integrador Final. Sistema de gestión de libros con autenticación segura y CRUD completo.

## 🚀 Funcionalidades
- Registro de usuarios con verificación por email
- Login con JWT (tokens con expiración)
- CRUD de libros (con relación al usuario que los crea)
- Arquitectura en capas: routes → controllers → services → repositories

## 🛠️ Tecnologías
- Node.js + Express
- MongoDB Atlas
- Bcrypt + JWT + Nodemailer
- Express Validator + CORS + Helmet

## ⚙️ Instalación
1. Cloná el repositorio:
   ```bash
   git clone https://github.com/valevallejos445/book-manager-backend.git
   cd book-manager-backend